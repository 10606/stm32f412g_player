#ifndef CHAR_SIZES_H
#define CHAR_SIZES_H

#include <stddef.h>

namespace sz
{

const size_t song_name = 28;
const size_t group_name = 28;

const size_t number = 5;
const size_t pl_name = 20;
const size_t count_offset = number + pl_name + 5;
const size_t count = 3;

const size_t volume = 10;

}
        
#endif

